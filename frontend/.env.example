# Base URL of the backend API this dashboard talks to.
# Exposed to the browser (NEXT_PUBLIC_*), so never put secrets in it.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# Used by docker-compose.yml when building/running the production image.
PORT=3000
